import ToggleDownload from '../../../../app/javascript/lib/viewer/transcripts_load.js';


it('toggles a download', () => {
  // const fetchPromise = new Promise((resolve, reject) => {
  //   resolve({text: () => '<div>Foo</div>'});
  // });

  // const fetcher = {
  //   fetch: () => fetchPromise
  // }

  // const transcriptsElem = {
  //   innerHTML: jest.fn()
  // }

  // TranscriptsLoad(10, transcriptsElem, fetcher)

  // expect(transcriptsElem.innerHTML.mock.calls).toEqual(["class", "col-md-5"]);
  // expect(transcriptsElem.mock.calls[0]).toEqual(["class", "col-md-7"]);
});
