<%   item_list.each do |item| %>
  <%= render 'item', item: item,
                     search_params: search_params,
                     thumbnail: Umedia::Thumbnail::Url.new(item: item) %>
<% end %>